specVersion: "v1beta"
serviceId: "vald-tour-admin"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "vald-tour-admin-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
